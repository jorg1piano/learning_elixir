defmodule SecretPassword do
  # A file with a list of turns
  def read_file(input_file) do
  end

  def extract_list_from_file_contents(file_contents) do
  end

  def find_password(list_of_turns) do
  end
end
